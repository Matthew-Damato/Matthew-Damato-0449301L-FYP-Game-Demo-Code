//This will handle the generation of the level proper.

using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LevelGenerator : MonoBehaviour
{
    // Give each mini-game a number value
    // Then each theme and asset selection would be selected.
    
    

    public bool like;

    //At start it will generate the 3 most likely levels and have the user pick one. 
    //The below function will handle using a learning algorithm to calculate the generation options.
    //This will take the given probability for each theme and game and select 3 combinations, which would tell the program what to load
    //The below function handles the generation of the 3 minigames. These will then be displayed on screen.
    public void MinigameGeneration()
    {

    }




    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    // 
}
